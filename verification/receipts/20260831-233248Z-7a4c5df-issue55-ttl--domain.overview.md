---
receipt: verification-receipt/v0
run_id: 20260831-233248Z-7a4c5df-issue55-ttl
feature_id: domain.overview
profile: changed
surface: web
sha: 7a4c5df6763c6ca9b1a5d8c6dfc7fe665f6eabd0
code_digest: 0a92ff85b33ec787ca88286b068d29e5c0c86b82bacbecdea3bb01cb4bab11ee
dirty: true
untracked: 2
status: blocked
reason: "No live database with a persisted snapshot containing public-recursive evidence in this environment (postgres absent locally), so the harness live attestation cannot run. Behavior proven by 17 fake-clock unit tests and 3 Playwright fixed-clock E2E tests against the real UI; live re-verification required where persisted evidence exists."
verifier: builder
verifier_session: ""
evidence_dir: verification/runs/20260831-233248Z-7a4c5df-issue55-ttl
created_at: 2026-08-31T23:32:58.357Z
---

# Receipt: domain.overview — blocked

## Observations (expected → seen)

- Unit (fake clock, `apps/web/app/lib/dns-ttl.test.ts`, 17 tests): fresh public-recursive evidence yields deadline = queriedAt + ttl×1000 and ceil-based remaining; 1 ms before deadline → 1, exact deadline → valid 0, after → stale; TTL 0 at queriedAt is a valid zero, not missing; missing/negative/fractional TTL, invalid/future queriedAt, failed or authoritative-only observations, and owner/type mismatches → unknown; latest deadline wins across recursive sources; synthesized record.ttl never used.
- E2E (`apps/web/e2e/domain-states.spec.ts`, Playwright fixed clock, 3 tests): DNS Parsed view renders Remaining TTL / Estimated live at headers, "300s remaining" cell, `<time datetime>` = queriedAt + 300 s ISO, ticks to 299 s, exact 0 at deadline, UNKNOWN ×2 (with expired/no-evidence titles) after the deadline and for authoritative-only evidence.
- Lint/typecheck/build/unit suites green; lint-map --fresh and lint-selectors at baseline parity (0 errors).

## Forbidden (must not happen → confirmed absent)

- No collector/API/schema/parsing/package/lockfile changes (git diff confined to the six owned files).
- No aria-live on the ticking value; no class/coordinate selectors added; no fixed sleeps in new tests.

## Read-back (side effects checked through an independent path)

- Live harness attestation (`web.mts` domain.overview drive: real observations endpoint + per-row cell audit) NOT run: this environment has no database with a persisted snapshot containing recursive evidence (no postgres on 5432), so a `passed` receipt is not claimable here. The drive code is in place for an environment with persisted evidence.

## Artifacts

| path | kind | check | sha256 |
|---|---|---|---|
| verification/runs/20260831-233248Z-7a4c5df-issue55-ttl/env.txt | env | aux | e0d528c6d413298bebf2980320549ee398178107f744be8c8baf71ae9521cf1f |
