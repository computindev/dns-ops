---
receipt: verification-receipt/v0
run_id: <<run id from run-new>>
feature_id: <<domain.capability>>
profile: <<changed | critical>>
surface: <<web | api | cli | worker | pipeline>>
sha: <<40-hex commit at verification time>>
code_digest: <<sha256 of the verified code tree, excluding verification/receipts/ — what CI matches>>
dirty: <<true | false — uncommitted tracked changes at verification time>>
untracked: <<count>>
status: <<passed | failed | blocked | unreachable | not_applicable>>
reason: "<<required unless passed>>"
verifier: <<builder | fresh>>
verifier_session: "<<harness:session id>>"
evidence_dir: verification/runs/<<run id>>
created_at: <<ISO-8601 UTC>>
---

# Receipt: <<feature id>> — <<status>>

## Observations (expected → seen)

- <<expected>> → <<what was actually observed, with the concrete value>>

## Forbidden (must not happen → confirmed absent)

- <<forbidden>> → <<how absence was confirmed>>

## Read-back (side effects checked through an independent path)

- <<endpoint / query / file>> → <<value>>

## Artifacts

| path | sha256 |
|---|---|
| <<generated by verify.mjs receipt>> | |

(Generate this file with `node .agents/verify-kit/verify.mjs receipt …` — it fills sha, dirty, timestamps and artifact hashes; you write the three sections above, ideally via `--notes-file`.)
